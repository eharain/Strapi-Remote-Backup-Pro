/**
 * `strapi-backup login --url <url> --email <email>`
 *
 * Verifies credentials, detects the Strapi version, and stores a profile. The
 * password is prompted for rather than accepted as a flag — a flag lands in the
 * shell history of a machine that also holds the backups.
 */
export {};
