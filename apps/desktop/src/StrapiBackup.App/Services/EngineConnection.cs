namespace StrapiBackup.App.Services;

/// <summary>
/// The app's handle on the engine: starts it at launch, exposes the typed client,
/// and surfaces engine death to the UI rather than letting calls hang.
/// </summary>
public sealed class EngineConnection;
